interface:
  display_name: "$BACKGROUND_GEN"
  short_description: "Layered mobile casual-game backgrounds with parallax, blur maps, and readability metadata."
  default_prompt: "Use $BACKGROUND_GEN only. This is image-first: create actual layered background images with built-in image generation before returning. Apply the fixed $BACKGROUND_GEN signature, active Style Lock tokens, gameplay-safe contrast, layered depth, crop-safe portrait/landscape variants, blur maps for menus, parallax metadata, and no pixel-art RPG maps or AAA-realistic environments."
