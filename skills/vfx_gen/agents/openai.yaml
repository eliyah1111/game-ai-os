interface:
  display_name: "$VFX_GEN"
  short_description: "Casual-game VFX frames, timing metadata, blend modes, and animation curves."
  default_prompt: "Use $VFX_GEN only. This is image-first: create actual transparent VFX frame images with built-in image generation before returning. Apply the fixed $VFX_GEN signature, active Style Lock tokens, transparent frames, short satisfying feedback timing, blend-mode metadata, animation curves, low-performance variants, no pixel art, and no effects that hide puzzle state too long."
